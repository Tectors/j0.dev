<UserControl
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fluent="clr-namespace:FluentIcons.Avalonia.Fluent;assembly=FluentIcons.Avalonia.Fluent"

    x:Class="vj0.Views.OnboardingTerms.OnboardingTermsMappingsView"
    
    mc:Ignorable="d"
    d:DesignWidth="1500"
    d:DesignHeight="1500">
    
    <Border Background="{DynamicResource GuideBackgroundBrush}"
            CornerRadius="12" 
            BorderBrush="#333333"
            BorderThickness="1"
            Margin="20 0"
            VerticalAlignment="Top"
            Padding="40">
        <StackPanel Spacing="10">
            <TextBlock FontSize="26" LetterSpacing="-1"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillPrimaryBrush}">
                What are Mappings?
                
                <fluent:FluentIcon
                    Icon="DataScatter"
                    IconSize="Size32"
                    IconVariant="Regular"/>
            </TextBlock>

            <TextBlock FontSize="19" 
                       TextWrapping="Wrap">
                .usmap files define how game data is structured, allowing it to be interpreted correctly.
            </TextBlock>
            
            <StackPanel Margin="10,5,0,0" Spacing="4" Opacity="0.7">
                <TextBlock FontSize="17" TextWrapping="Wrap">• Downloaded automatically when needed</TextBlock>
                <TextBlock FontSize="17" TextWrapping="Wrap">• Required if the game modifies UE's serialization</TextBlock>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>